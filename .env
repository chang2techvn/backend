# Database connection string (used by Prisma)
DATABASE_URL=postgresql://mission_brief:mission123@localhost:5432/task-management-api?schema=public

# Individual database connection parameters (used by the application)
DB_USER=mission_brief
DB_PASSWORD=mission123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=task-management-api

# Environment indicator
NODE_ENV=development

# If you're using macOS PostgreSQL default installation
# Try uncommenting this line instead if the above connection fails
# DATABASE_URL=postgres:///task-management-api?host=/tmp
